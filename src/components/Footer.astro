---
import { ui } from '@utils/i18n';
const { pathname } = Astro.url;
const isPt = pathname.includes('/pt');
const t = isPt ? ui.pt : ui.en;
const nome = "Michael Howe";
---

<footer style="margin-top: 100px; padding: 60px 2rem; border-top: 1px solid var(--card-border); text-align: center; background: #14171b">
    <h2 style="font-size: 2rem; margin-bottom: 20px;">Ready to start a project?</h2>
    <div style="display: flex; justify-content: center; gap: 30px; margin-bottom: 40px;">
        <a href="#" class="chat-link">GITHUB</a>
        <a href="#" class="chat-link">LINKEDIN</a>
    </div>
    <p style="color: var(--secondary); font-size: 0.9rem;">
        © 2026 {nome} • Based in Brazil, looking for global opportunities.
    </p>
</footer>

<style>
                .chat-link {
                    position: relative; text-decoration: none;
                display: inline-block; padding: 12px 24px;
                text-decoration: none;
                border: 2px solid var(--accent); border-radius: 50px;
                color: var(--accent); font-weight: bold; transition: all 0.3s ease;
            }
            .chat-link:hover {
                background: var(--accent); color: #111;
                box-shadow: 0 0 20px rgba(0, 255, 157, 0.4);
            }
</style>